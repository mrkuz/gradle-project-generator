spring.application.name = <%= name %>

cloud.enabled = false
spring.cloud.config.enabled = \${cloud.enabled}
spring.cloud.config.fail-fast = \${cloud.enabled}
spring.cloud.config.discovery.enabled = \${cloud.enabled}
eureka.client.enabled = \${cloud.enabled}
